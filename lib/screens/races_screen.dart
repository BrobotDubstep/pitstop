import 'package:flutter/material.dart';

class RacesScreen extends StatelessWidget {
  const RacesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Races"));
  }
}
